name = "blt-leaf"
main = "src/index.py"
compatibility_date = "2024-01-01"
compatibility_flags = ["python_workers"]

# D1 Database should be configured via Cloudflare Dashboard
# Navigate to: Workers & Pages > Your Worker > Settings > Variables
# Add D1 Database Binding with name "DB" pointing to your pr-tracker database

[vars]
ENVIRONMENT = "production"

# Configure assets (static files)
[assets]
directory = "./public"
